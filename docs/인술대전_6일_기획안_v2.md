# 인술대전_6일_기획안_v2

# 인술 대전 (가제) — 6일 웹앱 기획안 v2

**팀 구성**: 2인 / **기간**: 6일 / **플랫폼**: 웹 브라우저 (데스크톱 + 웹캠 필수)

> **v1(5일) 대비 변경 요약**
1. **12지신 전종 인식이 정식 목표로 승격** (v1에선 6종). 동시커밋형은 Day 6 조건부 스트레치로 강등.
2. **데이터 수집을 Day 1~3에 분산** — 이게 6일 계획의 핵심 설계 결정. 이유는 §4.3.
3. **인식기를 교체 가능 모듈로 고정** — 6종 룰 기반으로 게임을 먼저 완성하고, 나중에 12종 모델로 갈아끼움.
4. **Day 6 오후는 코드 프리즈 + 리허설 전용.** 새 기능 금지.
> 

---

## 1. 프로젝트 개요

가나디화(SD/치비)된 애니메이션 캐릭터를 픽하고, 웹캠으로 손을 인식해 닌자 인장(印)을 맺어 싸우는 1:1 대전 웹게임.

- **MVP (필수)**: **스피드 인술 맺기** — 서버가 제시한 인장 시퀀스를 두 플레이어가 웹캠 앞에서 실제 손으로 맺고, 먼저 완성한 쪽이 술법을 발동해 상대 체력을 깎는다. 체력 0 → 승리.
- **목표 (Day 5까지)**: **12지신 전종 인식**. 실제 게임 시퀀스에는 인식률이 검증된 인장만 투입 (§4.5).
- **스트레치 (Day 6 조건부)**: **동시 커밋형** — 각자 공격/방어를 인장으로 비밀 커밋 후 동시 공개 (BattleCON·Yomi식 심리전).

### 성공 기준 (Day 6 종료 시점)

1. 배포된 URL에서 서로 다른 PC 2대가 방 코드로 매칭 → 대전 1판 완주.
2. 12지신 인장이 도감·연출에 전부 등장하고, 실전 시퀀스에 투입된 인장의 인식률이 연습된 플레이어 기준 90%+.
3. 상대 얼굴/손이 보이는 화상 창 (렉이 있어도 게임 판정과 무관).
4. 캐릭터 선택 → 대전 → 승패까지 완결된 플로우.
5. 처음 보는 사람 앞에서 5분 시연이 사고 없이 돌아간다.

---

## 2. 기술 스택 (v1과 동일 — Phaser.js 채택)

Unity는 WebGL 빌드에서 손인식(네이티브 플러그인 불가)·WebRTC가 전부 우회 경로가 되어 브리지 작업에 1~2일이 소모될 리스크가 크다. 6일로 늘어나도 이 판단은 바뀌지 않는다 — 늘어난 하루는 Unity 브리지가 아니라 인식 정확도에 투자하는 게 결과물에 훨씬 크게 남는다.

| 역할 | 기술 | 비고 |
| --- | --- | --- |
| 손 인식 | **MediaPipe Tasks Vision — Hand Landmarker** (`num_hands: 2`) | WASM, 브라우저 내 추론, GPU 서버 불필요 |
| 인장 판별 | **6종 룰 기반 → 12종 소형 MLP로 교체** | §4. 교체 가능 모듈로 설계 |
| 모델 학습 | **TensorFlow.js** (브라우저 내 학습) | 파이프라인이 JS 하나로 끝나 배포·재학습이 간단 |
| 게임 엔진 | **Phaser 3** | 스프라이트·트윈·체력바·씬 관리 |
| 화상 | **WebRTC + PeerJS** (`getUserMedia`) | 소통 전용, 판정 미사용 |
| 게임 통신 | **Socket.IO** | 방/매칭/이벤트. 서버 권위 판정 |
| 서버 | **Node.js (Express + Socket.IO)** | 시그널링 + 심판 |
| 배포 | 클라이언트 Vercel/Netlify, 서버 Render/Fly.io | **HTTPS 필수** (getUserMedia 제약) |

**아키텍처 원칙**: 영상(WebRTC)과 판정(Socket.IO) 완전 분리. 인식은 각자 로컬에서 수행, 서버엔 “시퀀스 완성 @timestamp”만 전송. 서버 수신 순서로 승자 판정 → 넷코드 난이도 제로.

---

## 3. 게임 디자인 (MVP: 스피드 인술 맺기)

### 3.1 매치 구조

- 캐릭터 선택 (4캐릭, 스탯 동일·스킨만 차이 — 밸런싱 제거) → 방 생성/코드 입장 → 대전.
- 체력 100. 라운드마다 서버가 랜덤 시퀀스를 양쪽에 동시 제시.
- 시퀀스 길이로 난이도 조절: 3연쇄(데미지 20) / 5연쇄(데미지 40). 초반 3연쇄, 후반 5연쇄.
- 먼저 완성한 쪽이 술법 발동 + 상대 체력 감소. 동시 완성 시 서버 수신 타임스탬프 우선.
- 한 판 목표 시간: 2~3분.

### 3.2 인식 UX

- 목표 인장 아이콘 열 + 내 인식 상태 게이지. 올바른 인장을 **0.4초 홀드**하면 확정 + 이펙트/효과음.
- 오인식 페널티 없음 — 인식이 안 될 뿐. “빨리 정확히 맺는 실력”이 곧 게임 실력.
- 매치 전 **캘리브레이션 씬**: 손 위치 가이드 박스로 가슴 높이·정면 유도 + 개인 샘플 보정. 튜토리얼 겸용.

---

## 4. 인장 인식 설계 — 6일 계획의 핵심

### 4.1 왜 12지신이 어려운가

두 손을 깍지 껴 맞물리는 인장은 구별의 핵심 정보(어떤 손가락이 위/아래로 갔는가)가 손 안쪽에 가려진다. MediaPipe는 안 보이는 부분을 추측해 채우므로, **데이터를 아무리 모아도 입력에 정보가 없는 케이스가 남는다.**

| 유형 | 예시 계열 | 난이도 |
| --- | --- | --- |
| 수평 교차형 | 개, 원숭이 | **쉬움** — 실루엣이 완전히 다름 |
| 세로 + 손가락 돌출형 | 호랑이, 말, 쥐, 토끼 | **중간** — 돌출 손가락으로 구분 |
| 완전 맞물림형 | 뱀, 멧돼지, 용, 소 | **어려움** — 돌출 없이 안쪽 배열만 다름 |

12개 중 4~5개가 마지막 뭉치. 나머지 7~8개는 데이터만 있으면 잘 나온다.

### 4.2 판을 뒤집는 재구성: 분류가 아니라 “검증”

스피드 모드에선 목표 시퀀스가 화면에 떠 있다. 시스템이 답할 질문은 “12개 중 뭐냐”가 아니라 **“지금 이게 목표 인장 X 맞냐”** — 12-way 분류가 이진 검증으로 바뀌면 난이도가 크게 낮아진다.
- 부작용(대충 비슷한 포즈로 통과) 방지: **런너업 마진** 조건 — 1등 점수가 2등보다 임계값 이상 높을 때만 인정.

### 4.3 데이터 수집을 Day 1~3에 분산하는 이유 ★

v1에서 12지 확장을 “Day 4 오후에 몰아서”로 잡았는데, 6일 계획에선 **Day 1부터 매일 조금씩** 모으는 방식으로 바꾼다. 단순히 시간이 남아서가 아니라 데이터 품질이 실제로 더 좋아지기 때문:

- **하루에 몰아 찍으면 그날의 조명·옷·카메라 위치가 데이터에 통째로 각인된다.** 다른 날 다른 환경에서 무너짐.
- 며칠에 나눠 찍으면 그 분산이 공짜로 데이터셋에 들어간다. 같은 샘플 수로 일반화 성능이 더 좋다.
- 학습→혼동행렬 확인→약한 인장만 추가 수집의 **반복 사이클을 돌릴 시간**이 생긴다. 이게 정확도를 올리는 진짜 요인.
- **제3자 1명 이상 반드시 포함.** 팀원 2명 손으로만 학습하면 시연 때 심사위원 손에서 무너진다. Day 2~3 중 하루 외부인을 붙잡아 5분만 찍으면 됨.

### 4.4 거의 공짜로 정확도를 올리는 것들 (전부 적용)

- **시간축 다수결** — 10프레임 윈도우 투표. 단발 오인식이 사라짐.
- **쌍거리 특징** — 원시 좌표 대신 선택 랜드마크 쌍의 거리 행렬을 입력. 맞물림 구조를 훨씬 잘 인코딩.
- **손목 기준 정규화** — 위치·스케일 불변.
- **카메라 가이드** — 캘리브레이션 씬의 가이드 박스로 각도 분산 축소.

### 4.5 최종 안전장치 — “도감 12개, 실전 N개”

Day 5 혼동행렬에서 인식률이 안 나오는 2~3종은 **게임 시퀀스 생성에서만 제외**한다. 인장 도감·캐릭터 연출에는 12종 전부 등장. 사용자 입장에선 “12지신 게임”이 맞고, 실패율은 보이지 않는다.

### 4.6 교체 가능 모듈 계약 (Day 1에 고정)

`recognizer.js`는 내부 구현과 무관하게 `onSeal(sealId, confidence)`만 발행한다. 6종 룰 기반 → 12종 MLP 교체가 **파일 교체 수준**이 되도록. 이 계약 덕분에 12종이 실패해도 6종 버전이 그대로 살아 있어 손해가 0.

---

## 5. 역할 분담 (2인)

|  | **A: 인식·게임플레이** | **B: 네트워크·화면** |
| --- | --- | --- |
| 핵심 소유 | MediaPipe 파이프라인, 데이터 수집·학습, 인장 판별기, 게임 룰 | Node 서버(방·심판), Socket.IO, PeerJS 화상, Phaser 씬/UI, 배포 |
| 산출물 | `recognizer.js`, 라벨링 툴, 학습된 모델, 로컬 연습 모드 | 로비→대전→결과 플로우, 배포된 서버 |

인터페이스 계약(§4.6)을 Day 1에 고정 → Day 2~3 완전 병렬 작업.

---

## 6. 일별 계획

> 각 날은 **완료 기준(DoD)** 포함. DoD를 못 맞추면 일정을 미루지 말고 다음 날 기능을 자른다.
> 

### Day 1 — 기술 검증 + 수집 툴

- **A 오전**: MediaPipe Hand Landmarker 붙여 두 손 42 랜드마크 실시간 시각화. 손목 정규화 + 쌍거리 특징 추출 유틸.
- **A 오후**: **같은 페이지를 라벨링/수집 툴로 확장** (인장 선택 → 스페이스바 → N프레임 캡처 → JSON 저장). 툴이 완성되는 즉시 **12종 1차 수집**. 병행해서 쉬운 6종 룰 기반 판별 작성 (Day 2~3 게임용 임시 인식기).
- **B 오전**: 모노레포 셋업(`client/` + `server/`) + 배포 파이프라인. Node + Socket.IO 서버 골격: 방 생성/코드 입장/2인 매칭.
- **B 오후**: Phaser 3 부트스트랩, 씬 구조(로비/대전/결과) 골격. `getUserMedia` 프리뷰를 Phaser 캔버스 옆 DOM에 표시.
- **공동 (막판 1시간)**: `onSeal(sealId, confidence)` 계약 확정. 두 브라우저 탭이 같은 방에 접속 확인.
- **DoD**: ① 수집 툴 동작 + 12종 1차 데이터 확보 ② 6종 룰 기반이 안정 인식 ③ 두 탭이 방에서 메시지 교환.

### Day 2 — 1인 연습 모드

- **A**: 인식기 안정화(0.4초 홀드, 시간축 다수결, 15~20fps 스로틀, 인식 게이지). 시퀀스 진행 로직. **(20분) 12종 2차 수집 — 다른 조명/시간대.**
- **B**: 대전 씬 UI — 목표 인장 아이콘 열, 양측 체력바, 내/상대 진행 표시, 카운트다운. 임시 스프라이트로 캐릭터 배치.
- **공동**: 합쳐서 **1인 연습 모드 완성** — 시퀀스 제시 → 손으로 맺으면 진행 → 완성 시 이펙트 + 가상 상대 체력 감소.
- **DoD**: 혼자 한 판 완주 가능. **이 시점에 이미 시연 가능한 데모가 하나 존재.**

### Day 3 — 온라인 대전 ★ 절대 사수 라인

- **B 오전**: 서버 심판 로직 — 라운드 상태 머신(대기→시퀀스 배포→수신 대기→판정→데미지 브로드캐스트→다음 라운드/종료). 시퀀스는 서버 생성.
- **A 오전**: 클라이언트를 서버 권위 구조로 전환. 로컬 판정 제거, 완성 이벤트 송신 + 판정 수신 반영. 상대 진행 상황 실시간 표시.
- **오후 공동**: PC 2대 실대전 테스트. 이탈 시 몰수승 처리(재접속 복구는 스코프 아웃), 동시 완성 타이브레이크 확인. **(20분) 12종 3차 수집 — 제3자 1명 포함 필수.**
- **DoD**: 서로 다른 PC 2대에서 매칭 → 판정 → 승패 완주. **여기까지 못 오면 12지신 확장은 즉시 포기하고 6종으로 확정.**

### Day 4 — 화상 + 모델 v1

- **A 종일**: 수집 데이터로 **소형 MLP 학습 (TF.js)** → **혼동행렬 산출** → 약한 인장 식별 → 해당 인장만 표적 추가 수집 → 재학습. 이 사이클을 2~3회 반복.
- **B 오전**: PeerJS 화상 — 매칭 시 peer id를 Socket.IO로 교환 → P2P 영상. 게임 화면 옆 상대 캠. **실패해도 게임은 정상 진행되도록 격리.**
- **B 오후**: 캐릭터 선택 씬(4종 스킨), 술법 발동 이펙트(파티클/화면 흔들림/효과음), 승패 연출.
- **DoD**: ① 12종 모델 v1의 혼동행렬 확보 — 어느 인장이 몇 %인지 숫자로 안다 ② 화상 연결 성공(또는 컷 결정).

### Day 5 — 인식기 교체 + 배포

- **A 오전**: 12종 모델을 `recognizer.js`에 통합. 런너업 마진·인장별 임계값 튜닝. **6종 룰 기반은 지우지 말고 폴백 플래그로 남긴다.**
- **A 오후**: 실전 투입 인장 확정 (§4.5) — 기준 미달 2~3종은 시퀀스 생성에서 제외. 인장 도감 UI에는 12종 전부 노출.
- **B 오전**: 실배포(HTTPS). 외부 네트워크 간 테스트.
- **B 오후**: 캘리브레이션/튜토리얼 씬(가이드 박스 포함), 사운드, 버그 픽스.
- **DoD**: 배포 URL에서 2대 PC가 화상 보며 12지신 대전 완주. **이 시점이 사실상 완성.**

### Day 6 — 게이트 + 코드 프리즈

- **오전 게이트 판정**: Day 5 DoD가 전부 안정적인가?
    - **불안정** → 온종일 버퍼. 인식률 재튜닝, 버그 픽스, 밸런스(시퀀스 길이·데미지).
    - **안정** → **동시 커밋형 미니 구현** (§7). 기존 인프라 전면 재사용이라 반나절이 현실적.
- **오후 (무조건)**: **코드 프리즈.** 새 기능 금지. 시연 리허설 2회 이상, README + 시연 대본 작성, 백업 환경 점검(서버 워밍업, 예비 노트북, 조명 확인).
- **DoD**: 처음 보는 사람 앞 5분 시연이 사고 없이 완주.

---

## 7. 스트레치: 동시 커밋형 (Day 6 조건부)

- **라운드 구조**: 10초 커밋 타임 — 공격 3종(불/물/번개) 또는 방어 3종 중 하나를 인장으로 비밀 커밋 → 3·2·1 동시 공개 → 판정.
- **판정 (밸런싱 최소화)**: 공격 vs 잘못된 방어 = 명중(20) / 공격 vs 올바른 방어 = 무효 + 반격(10) / 공격 vs 공격 = 쌍방 명중 / 방어 vs 방어 = 무효. 가위바위보 상성만으로 시작.
- **재사용률**: 인식기·서버 방·화상·UI 전부 그대로. 새로 필요한 건 커밋 상태 머신 + 동시 공개 연출 + 상성 테이블뿐.
- **인식 부담 완화**: 스피드 경쟁이 아니므로 홀드 시간을 0.8초로 늘려 오인식 리스크 축소.

---

## 8. 리스크 & 컷 전략

| 리스크 | 감지 시점 | 대응 |
| --- | --- | --- |
| Day 3 온라인 대전 미완 | Day 3 저녁 | **12지신 즉시 포기**, 6종으로 확정하고 Day 4~5를 완성도에 전량 투입 |
| 12종 모델 정확도 미달 | Day 4 혼동행렬 | 실전 인장을 9~10종으로 축소(§4.5). 그래도 안 되면 폴백 플래그로 6종 복귀 |
| 특정 인장이 끝까지 안 됨 | Day 5 | 도감에만 남기고 시퀀스 제외. 사용자에겐 안 보임 |
| 데이터가 팀원 손에만 과적합 | Day 4 | Day 3에 제3자 수집을 못 했으면 Day 4 오전 최우선으로 확보 |
| P2P 화상 NAT 실패 | Day 4 | 화상 컷, 외부 통화(디스코드) 병행. 원칙상 게임엔 무영향 (TURN은 스코프 아웃) |
| 무료 서버 콜드스타트 | Day 5 | 판정이 서버 도착 순서라 공정성 유지. 시연 전 워밍업 |

**컷 우선순위**: 동시커밋형 > 화상 > 캐릭터 연출 > 12지신 확장 > **온라인 대전(절대 사수)**

## 9. 저작권 메모

실제 애니메이션 캐릭터를 그대로 쓰면 공개 배포 시 문제가 될 수 있으니 “닌자 컨셉 오리지널 SD 캐릭터” 또는 무료 에셋 사용을 권장. 인장 명칭(십이지) 자체는 사용에 문제 없음.