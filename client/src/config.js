// 클라이언트 설정값 모음.

// import.meta.env는 Vite가 주입한다. `?.`를 쓰는 이유: tools/의 node 스크립트가
// 이 파일을 그대로 import해서 인식 코드를 오프라인 검증하기 때문 (node엔 env가 없다).
export const SERVER_URL = import.meta.env?.VITE_SERVER_URL ?? 'http://localhost:3001';

export const GAME = {
  WIDTH: 1280,
  HEIGHT: 720,
};

export const RECOGNITION = {
  NUM_HANDS: 2,

  // --- 판별 임계값 (단위: 90차원 특징공간에서의 유클리드 거리) ---
  // ⚠️ 확률이 아니라 거리다. 값이 작을수록 "센트로이드에 가깝다 = 확실하다".
  //
  // 2026-07-27 2차 수집 후 확정. "인장 아님" 샘플 30장을 함께 찍어 근거를 숫자로 잡았다:
  //   인장 샘플 거리   : 최소 0.06 · 중앙 1.36 · 최대 4.50
  //   "인장 아님" 거리 : 최소 5.39 · 중앙 6.95
  //   → 4.50과 5.39 사이가 비어 있다. 4.0이면 인장의 99.6%를 받으면서
  //     가장 가까운 오탐 후보와도 1.39 여유가 남는다.
  // MARGIN은 1.0 → 0.5. 오탐은 거리로 이미 걸러지므로, 마진을 조이면
  // 정상 인장만 죽는다 (1.0에서 92.4% → 0.5에서 96.0%, 오탐률은 양쪽 0%).
  ACCEPT_THRESHOLD: 4.0, // 가장 가까운 센트로이드까지 이보다 멀면 "인장 아님"
  MARGIN: 0.5,           // 1등이 2등보다 이만큼 더 가까워야 인정 (런너업 마진, §4.2)

  // --- 안정화 ---
  VOTE_WINDOW: 8,   // 시간축 다수결 윈도우 (프레임). §4.4
  FPS_THROTTLE: 20, // 추론 스로틀 (Step 7에서 적용). 이 값을 낮추면 VOTE_WINDOW도 같이 줄여야
                    // 다수결이 실시간 기준으로 길어지지 않는다.
};

// --- MLP 임계값 (단위: 0~1 확률) ---
// ⚠️ 위 RECOGNITION의 값과 단위가 다르다. 저쪽은 거리(작을수록 확실), 이쪽은 확률(클수록 확실).
//
// 2026-07-28 제3자 세션 홀드아웃으로 스윕. ACCEPT 0.50~0.95 × MARGIN 0.20~0.80,
// 24개 조합 전부 오탐률이 동일하게 나왔다 — 오탐하는 'none' 샘플이 확신도 1.00으로
// 들어오기 때문이다. 즉 여기서 조여봐야 오탐은 안 줄고 정상 인장만 죽는다.
// → 인장 정답률이 최고인 구간에서 가장 보수적인 값을 골랐다.
//   진짜 해결책은 'none' 샘플 다양화 수집이고, 그 뒤에 이 값을 다시 스윕할 것.
export const MLP = {
  ACCEPT: 0.80, // 1등 확률이 이보다 낮으면 "모르겠다"
  MARGIN: 0.20, // 1등이 2등보다 이만큼 앞서야 인정
};

// 홀드 확정 시간은 shared/constants.js의 RULES.SEAL_HOLD_MS(400) 하나만 본다.
// 서버·클라가 같은 값을 봐야 하므로 여기에 중복 정의하지 않는다.
